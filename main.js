(function () {
    // Declare variables here
    // currentPosition
    // currentDirection
    
    /**
     * takes a distance parameter. 
     * Move the character forward (based on the 
     * direction they are facing) the specified distance.
     */
    // moveForward(distance)

    /**
     * takes a distance parameter. Move the character backward 
     * (based on the direction they are facing) the specified distance.
     */
    // moveBackward(distance)

    /**
     *  reverse the direction the character is facing.
     */
    // turnAround()

    /**
     * logs the current position to the console
     */
    // printLocation()

    // // Uncomment these after your functions are written
    // moveForward(5)
    // moveBackward(3)
    // printLocation() // should print (2)
    // turnAround()
    // moveForward(10)
    // moveBackward(5)
    // printLocation() // should print -3
})();